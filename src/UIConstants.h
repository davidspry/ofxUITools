//  ofxUITools
//  Created by David Spry on 27/1/21.

#ifndef UICONSTANTS_H
#define UICONSTANTS_H

/// @brief 2D axes.

enum class Axis { X, Y };

/// @brief Basic directions.

enum class Direction { N, E, S, W };

/// @brief Constants defining the vertical alignment of an element within a context.

enum class VerticalAlignment { Centre, Top, Bottom };

/// @brief Constants defining the horizontal alignment of an element within a context.

enum class HorizontalAlignment { Centre, Left, Right };

#endif
